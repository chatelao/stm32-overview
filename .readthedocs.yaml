version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

# Configure ReadTheDocs to compile the documentation.
mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: requirements.txt
