version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

# Configure ReadTheDocs to compile the documentation.
# By default, RTD supports Sphinx or MkDocs.
mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      path: .
